// Sum (+)
var sum = 5 + 5;

// Subtraction (-)
var difference = 10 - 5;

// Multiplication (*)
var product = 10 * 5;

// Division (/)
var quotient = 10 / 5;

// Remainder (%)
var remainder = 12 % 5;

// Exponentiation  (**)
var exponentiation  = 10 ** 2;

// Incrementing number (++) ps: ++ = 1
var increment = 10;
increment++;

// Decrementing number (--) ps: -- = -1
var decrement = 10;
decrement--;

// Decimal/Float numbers (.)
var decimal = 10.5;

// Compound Assignment (+=, -=, *=, /=, %=)
var compoundPlus = 10, compoundMinus = 20, compoundMultiply = 5, compoundDivide = 30, compoundRemainder = 10;
compoundPlus += 10;
compoundMinus -= 10;
compoundMultiply *= 10;
compoundDivide /= 5
compoundRemainder %= 4;

// Create a variable with string value
var myFirstName = 'Ryan', myLastName = 'Araújo';

// Escape Character (\) or Use single quotes ('') or Use a backticks (`) to use both of them at the same time
var myStr = "I'm a \"double-quoted\" string.";
myStr = 'I\'m a "double-quoted" string.';
myStr = `I'm a "double-quoted" string.`;
console.log(myStr);
// All escape characters 
/*

*/

